# Production build only (vite build). Dev keeps the workers.dev default from client.js.
# The SPA is served from the same Worker as the API, so same-origin /api/* — no CORS in prod.
VITE_API_URL=https://vymotion.org
# Clerk PRODUCTION instance (ins_3GJGQPhHZGNRkihXWfjMsiE1gV2, clerk.vymotion.org).
# Publishable key only — it's public by design; the secret key lives in Worker secrets.
VITE_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsudnltb3Rpb24ub3JnJA
